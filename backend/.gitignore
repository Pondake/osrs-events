node_modules
# Keep environment variables out of version control
.env

dist

# Prisma client, regenerated by the postinstall hook. The path is
# src/generated/prisma (schema.prisma sets generator.output) — the old
# /generated/prisma rule was anchored to backend/ and never matched, so
# the client was being committed.
/src/generated/prisma/
