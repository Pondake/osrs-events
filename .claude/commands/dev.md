Start both the backend and frontend dev servers.

1. In `backend/`, run `pnpm start:dev` in the background.
2. In `frontend/`, run `pnpm dev` in the background.
3. Report the ports both are running on (backend default: 3001, frontend default: 3000).
