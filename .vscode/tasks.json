{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "pnpm dev",
            "type": "shell",
            "command": "pnpm",
            "args": ["run", "dev"],
            "options": { "cwd": "${workspaceFolder}" },
            "isBackground": true,
            "problemMatcher": []
        },
        {
            "label": "artisan serve",
            "type": "shell",
            "command": "php",
            "args": ["artisan", "serve"],
            "options": { "cwd": "${workspaceFolder}" },
            "isBackground": true,
            "problemMatcher": []
        }
    ]
}
